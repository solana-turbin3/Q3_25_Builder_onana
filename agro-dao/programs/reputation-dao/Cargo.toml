[package]
name = "reputation-dao"
version = "0.1.0"
description = "Reputation utility service for AgroDAO ecosystem"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "reputation_dao"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"

